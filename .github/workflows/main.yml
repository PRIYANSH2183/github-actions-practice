name: My Workflow

on:
  push:

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello from Job 1"

  job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - name: Print Bye
        run: echo "Bye from Job 2"
